defmodule PostgresListener do

  @moduledoc """
  The following module is the root of the project. This project includes starting listener processer instead of being a direct entension. Hence the root module stays empty and processes are being taken care by the supervisor and the registry modules.
  """

end
